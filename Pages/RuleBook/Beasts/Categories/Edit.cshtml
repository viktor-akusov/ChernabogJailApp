@page
@model ChernabogJailApp.Pages.RuleBook.Beasts.Categories.EditModel

@{
    ViewData["Title"] = $"Редактировать категорию \"{Model.BeastCategory.Name}\"";
}

<h1>Редактировать категорию существ</h1>

<h4 class="d-flex flex-row gap-3">
    <span style="text-align: right;">@Html.DisplayFor(modelItem => Model.BeastCategory.Name)</span>
</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BeastCategory.Id" />
            <div class="form-group">
                <label asp-for="BeastCategory.Name" class="control-label"></label>
                <input asp-for="BeastCategory.Name" class="form-control" />
                <span asp-validation-for="BeastCategory.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BeastCategory.Description" class="control-label"></label>
                <textarea asp-for="BeastCategory.Description" class="form-control"></textarea>
                <span asp-validation-for="BeastCategory.Description" class="text-danger"></span>
            </div>
            <div class="form-group d-flex flex-row justify-content-between pt-2">
                <a href='javascript:window.location = document.referrer'>
                    <i class="btn btn-dark fa fa-arrow-left" aria-hidden="true"></i>
                </a>
                <input type="submit" value="Сохранить" class="btn btn-primary align-self-baseline" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
